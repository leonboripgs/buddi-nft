{
  "name": "buddi-core",
  "version": "1.0.0",
  "description": "Buddi Runner Contracts",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile": "truffle compile",
    "mainnet-migrate": "truffle migrate --network main --reset",
    "mainnet-verify": "truffle run verify BuddiNFT, BuddiCollection --network mainnet",
    "mainnet-all": "truffle migrate --network mainnet --reset && truffle run verify BuddiNFT, BuddiCollection --network mainnet",
    "development": "truffle migrate --network development --reset",
    "ropsten-migrate": "truffle migrate --network ropsten --reset",
    "ropsten-verify": "truffle run verify BuddiNFT, BuddiCollection --network ropsten",
    "ropsten-all": "truffle migrate --network ropsten --reset && truffle run verify BuddiNFT, BuddiCollection --network ropsten",
    "mainnet": "truffle migrate --network main --reset",
    "test": "truffle test"
  },
  "repository": "",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^4.6.0",
    "@truffle/hdwallet-provider": "^1.7.0",
    "bn.js": "^5.2.0",
    "dotenv": "^5.0.1",
    "ethers": "^5.5.4",
    "fs": "0.0.1-security",
    "ganache-cli": "^6.1.2",
    "mocha": "^9.2.1",
    "truffle": "^5.5.2",
    "truffle-assertions": "^0.9.2",
    "web3": "^1.7.0"
  },
  "devDependencies": {
    "truffle-plugin-verify": "^0.5.21"
  }
}
